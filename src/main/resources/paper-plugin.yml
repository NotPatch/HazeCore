name: HazeCore
version: ${project.version}
main: com.notpatch.hazeCore.HazeCore
api-version: '1.21'

dependencies:
  server:
    LuckPerms:
      load: BEFORE
      required: true
      join-classpath: true
    Vault:
      load: BEFORE
      required: true
      join-classpath: true
    PlaceholderAPI:
      load: BEFORE
      required: true
      join-classpath: true

loader: com.notpatch.hazeCore.loader.LibraryLoader